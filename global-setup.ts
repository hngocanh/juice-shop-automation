/**
 * Global setup hook — reserved for future use (e.g. shared auth/session seeding,
 * test data bootstrap, or API tokens applied across projects).
 */
async function globalSetup(): Promise<void> {
  // Intentionally empty.
}

export default globalSetup;
